@model Hall

    <!-- Header -->
    <div class="flex items-center justify-between pb-3 border-b border-base-300">
        <h3 class="text-xl font-bold">Create Hall</h3>

        <label for="modal" class="btn btn-sm btn-ghost btn-circle hover:bg-base-300">
            ✕
        </label>
    </div>

    <!-- Form -->
    <form class="flex flex-col gap-4 mt-4"
          id="createForm"
          method="post"
          asp-area="Admin"
          asp-controller="Hall"
          asp-action="Upsert"
          data-submit-url="/Admin/Hall/Upsert">

        @Html.AntiForgeryToken()

        <!-- Validation -->
        <div asp-validation-summary="ModelOnly"
             class="text-error text-sm bg-error/10 p-2 rounded-lg">
        </div>

        <input asp-for="Id" type="hidden" />

        <!-- Name -->
        <label class="form-control w-full">
            <span class="label-text font-medium">Hall Name</span>
            <input asp-for="Name"
                   type="text"
                   class="input input-bordered w-full focus:outline-none focus:ring-2 focus:ring-primary"
                   placeholder="Enter hall name"
                   autofocus />
            <span asp-validation-for="Name" class="text-error text-sm mt-1"></span>
        </label>

        <!-- Total Seats -->
        <label class="form-control w-full">
            <span class="label-text font-medium">Total Seats</span>
            <input asp-for="TotalSeats"
                   type="number"
                   min="1"
                   class="input input-bordered w-full focus:outline-none focus:ring-2 focus:ring-primary"
                   placeholder="Enter number of seats" />
            <span asp-validation-for="TotalSeats" class="text-error text-sm mt-1"></span>
        </label>

        <!-- Helper Text -->
        <p class="text-xs text-gray-500">
            Make sure the total seats reflect the actual hall capacity.
        </p>

        <!-- Actions -->
        <div class="modal-action mt-2 flex justify-end gap-3">

            <label for="modal"
                   class="btn btn-ghost border border-base-300 hover:bg-base-200">
                Cancel
            </label>

            <button class="btn btn-primary px-5 hover:brightness-110"
                    type="submit">
                Create Hall
            </button>

        </div>

    </form>
